function [chydro, cair, Batt_SOC, Batt_SOH] = consumption_(out)
    Ifc = out.FC.Ifc.Data;
    t = out.tout;
    N = 50; % Number of cells
    F = 96485; % Faraday constant
    nf = N/F;
    temp = nf*Ifc;
    
    chydro = trapz(t,temp);
    cair = 0;
    Batt_SOC = out.Batt.SOC____.Data(end);
    
    temp_soh = 
    Batt_SOH = trapz(t,out.Batt.Current__A_.Data)/14; % 14 = rated capacity
end
